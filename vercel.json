{
  "version": 2,
  "buildCommand": "npx prisma generate && next build --legacy-peer-deps",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "true"
  }
}
